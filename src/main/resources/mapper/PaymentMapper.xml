<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.PaymentMapper">
    <insert id="insertPayment" parameterType="com.javalab.board.vo.Payment">
        INSERT INTO payment (payment_id, jobPost_id, amount, payment_date, status)
        VALUES (#{paymentId}, #{jobPostId}, #{amount}, #{paymentDate}, #{status})
    </insert>

    <!-- 기타 쿼리 -->
</mapper>
