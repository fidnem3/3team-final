<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.JobPostMapper">
    <insert id="insertJobPost" parameterType="com.javalab.board.vo.JobPost">
        INSERT INTO jobPost (jobPost_id, comp_id, title, content, position, salary, experience, education, address, end_date, homepage, created, hit_no, status, logo_path, payment_id)
        VALUES (#{jobPostId}, #{compId}, #{title}, #{content}, #{position}, #{salary}, #{experience}, #{education}, #{address}, #{endDate}, #{homepage}, #{created}, #{hitNo}, #{status}, #{logoPath}, #{paymentId})
    </insert>

    <update id="updateJobPostStatus" parameterType="map">
        UPDATE jobPost
        SET status = #{status}
        WHERE jobPost_id = #{jobPostId}
    </update>

    <select id="findById" parameterType="Long" resultType="com.javalab.board.vo.JobPost">
        SELECT * FROM jobPost WHERE jobPost_id = #{jobPostId}
    </select>

    <select id="findAllApproved" resultType="com.javalab.board.vo.JobPost">
        SELECT * FROM jobPost WHERE status = 'Approved'
    </select>

    <!-- 기타 쿼리 -->
</mapper>
